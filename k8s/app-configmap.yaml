apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  APP_NAME: "tasktracker-app"
  LOG_LEVEL: "info"
  ENVIRONMENT: "production"
  DATABASE_HOST: "db-service"
  DATABASE_PORT: "5432"